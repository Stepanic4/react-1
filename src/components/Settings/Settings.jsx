import React from 'react';
import s from './Settings.module.css';

const Settings = (props) => {
  return (
      <div>
          <b className={s.item}>Settings</b>
      </div>
  )
};

export default Settings;